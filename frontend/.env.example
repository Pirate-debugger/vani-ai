# Copy to .env and set your values
# In development: leave VITE_API_URL commented (Vite proxy handles it)
# In production / APK build: set VITE_API_URL to your deployed backend
# VITE_API_URL=https://your-backend-url.com/api
